# PyLight #15 - Webscraping talk


## How to run
1. Create new virtualenv `mkvirtualenv pylight-talk`
2. Install dependencies `pip install -r requirements.txt`
3. Open `pylight.ipynb` in jupyter notebook

